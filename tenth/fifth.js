// number型で実装する
class Limit {
  constructor(number){
    this.number = parseFloat(number);
  }
  static MinMax(number){
    console.log(number.MIN_VALUE);
    console.log(number.MAX_VALUE);
    return
  }
}